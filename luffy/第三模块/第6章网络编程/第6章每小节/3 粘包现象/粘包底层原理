#1：不管是recv（接收）还是send（发送）都不是直接接收对方的数据，而是操作自己的操作系统内存--->不是一个send对应一个recv
#2：recv：两个阶段
        1、wait data 耗时非常长，
        2、copy data #操作系统把数据copy给应用程序

    send：#应用层序
        copy data # 把应用程序数据copy给操作系统，操作系统掉网卡发送

#3：tcp协议，具体怎么发，解决粘包：明确知道客户端的数据量，告诉服务端 ---长度----